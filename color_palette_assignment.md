色彩调色板工具
开发一款小型网页工具，支持输入基础色，并生成实用、展示效果良好的色彩变体。
表格
预计耗时	难度	交付物
4–5 小时	简单–中等	可访问链接 + 代码仓库 + README 文档
目标
开发一款小型工具，允许用户输入基础色，生成实用的色彩变体。重点关注功能准确性、清晰的界面设计，以及对边界情况的妥善处理。
需求说明
01 用户输入
提供一个文本输入框，供用户输入十六进制（HEX）颜色并提交。输入内容需先完成验证，再执行后续操作。
必须包含
支持以下所有合法格式：#E34F5B、E34F5B、#e34f5b、e34f5b
支持 3 位简写格式（如 #FFF），处理前自动扩展为 6 位格式（#FFFFFF）
去除首尾空白字符
拒绝非法输入，并显示清晰、常驻的行内错误提示 —— 无静默失效，无 2 秒后消失的轻提示
空提交需显示友好提示 “请输入十六进制颜色”，而非程序崩溃或生成空调色板
02 颜色转换
输入验证通过后，计算并展示三种格式的颜色值。输入内容变更时，三种格式需实时同步更新。
必须包含
HEX：统一为大写、6 位格式，带 # 前缀（如 #E34F5B）
RGB：三个 0–255 范围的整数，格式为 rgb(227, 79, 91)
HSL：色相为 0°–360°，饱和度与明度为 0%–100%，格式为 hsl(355°, 73%, 60%)
界面中 HSL 值取整数（调色板计算时保留内部高精度）
采用标准数学公式计算，无肉眼估算、无近似值
03 生成调色板
基于输入颜色，并排（或分页 / 分区域）生成三组命名调色板，每组恰好包含 5 种颜色。
必须包含
单色版（Monochrome）：5 种颜色色相相同，明度（可选饱和度）按均匀、可控的步长变化
邻近色版（Analogous）：以输入色为中心，色相分别偏移 −30°、−15°、0°、+15°、+30° 生成 5 种颜色
互补色版（Complementary）：围绕输入色及其 180° 互补色生成 5 种颜色（如输入色、浅输入色、互补色、浅互补色、中性色）
输入内容变更时，三组调色板实时重新生成
输入色（或其明确衍生色）需出现在每组调色板中
每组内 5 种颜色视觉上可区分，无完全相同的色样
04 色彩协调
调色板的计算逻辑需足够精准，使结果均衡协调，而非随机杂乱。这是专业调色板工具与普通取色器的核心区别。
必须包含
色相偏移在 HSL 色彩空间计算，超过 360° 时正确循环（如 350°+30°=20°，而非 380°）
邻近色严格限制在输入色相 ±30° 范围内
互补色采用精确 180° 偏移，而非 “大致相反”
单色版变体色相与输入色偏差 ≤±5°（仅允许调整明度 L 与饱和度 S）
明度与饱和度步长均匀分布，无扎堆、无断层
#FFFFFF、#000000 与纯灰色仍需生成 5 种视觉可区分、可使用的颜色，不合并为单一色调
README 文档说明所使用的计算公式
05 颜色命名（覆盖全部 16,777,216 种颜色）
为每一种生成的颜色生成唯一、可读的名称。命名映射需覆盖完整 24 位色彩空间（256×256×256=16,777,216 个十六进制颜色值）。
必须包含
24 位空间内唯一，无两个十六进制值共用同一名称
由代码自动生成，非人工整理的对照表
确定性：同一颜色始终生成同一名称
长度为 3–5 个单词
可读且有意义：名称需体现色相、明度、饱和度，红色不可命名为 “森林绿”
无针对特定颜色的硬编码判断（如无 if hex == "#E34F5B" => "…"）
README 文档说明命名规则（如组合规则：色相区间 × 饱和度区间 × 明度区间 × 后缀词）
示例（格式自定）
#E34F5B：暖珊瑚光
#8FD1ED：柔天青蓝
#222222：深炭黑影
#C89F6A：金秋沙棕
#6A4C93：深冬紫雾
不可接受
蓝色
深绿
Color_27
RGB_182_44_91
06 色样界面
每个生成的颜色以可点击色样展示。色样是工具的核心界面单元，需设计完善。
必须包含
每个色样展示：大面积色块、生成名称、HEX 值、RGB 值、HSL 值
点击色样可复制 HEX 值到剪贴板
复制后显示明确的确认提示（轻提示、对勾、色块闪烁均可，需显示 ≥1 秒）
色样文字保持可读：根据色样明度自动切换前景文字为深色 / 浅色
适配移动端布局（≥360px 宽度）：窄屏时色样垂直堆叠
07 验证与边界情况
工具需兼容所有输入场景，包括易被忽略的边界情况。
必须包含
空输入：显示友好行内错误
非法字符或长度错误：显示友好错误提示，说明合法十六进制格式
#FFFFFF 与 #000000 在所有调色板中仍生成 5 种可区分、可使用的颜色
纯灰色（R=G=B）不导致命名或调色板计算崩溃（灰色无 HSL 色相，需妥善处理）
若计算会生成两个相同颜色，通过确定性微调确保 5 种颜色全部唯一
全大写、全小写、混合大小写输入输出结果一致（如 #aBcDeF ≡ #ABCDEF）
08 作品预览
使用当前调色板的5 种颜色渲染小型作品。这是验证调色板实际可用性的关键，也体现设计审美。
必须包含
作品仅使用当前调色板的 5 种颜色，无额外颜色、无调色板外灰色、无透明度作弊混入新色调
调色板中每种颜色均在作品中使用，无闲置颜色
基础色或调色板类型变更时，作品实时重新渲染
为刻意设计的场景、插画、海报或界面原型，而非 5 个抽象矩形
极限输入（#FFFFFF、#000000、纯灰色）仍可渲染可识别的画面
README 文档说明所选作品类型，以及为何能很好展示调色板效果
09 部署
提交内容需可在浏览器直接访问，无需本地配置即可评估。
必须包含
部署到公开可访问链接（Vercel、Netlify、GitHub Pages、Cloudflare Pages 等），基于 HTTPS 协议
在线链接置于 README 文档顶部
最新版 Chrome 浏览器打开页面无控制台报错
支持 360px 移动端视口宽度正常使用
README 固定测试用例
README 需使用以下三种颜色演示工具效果：
#E34F5B：暖珊瑚红
#8FD1ED：柔天青蓝
#222222：近黑炭色
README 必须包含
工具在线访问链接（置于顶部）
本地运行方法
HEX→RGB→HSL 转换实现逻辑
单色版、邻近色版、互补色版生成规则，及色彩协调保障方式
命名规则如何覆盖全部 16,777,216 种颜色、保证唯一性与确定性，以及名称如何体现色相、明度、饱和度
所选预览作品说明，及展示调色板的原因
已知实现局限
时间充裕时的优化方向
评分标准
表格
评分项	分值
十六进制验证与标准化	5
RGB/HSL 转换准确性	10
调色板生成逻辑	10
色彩协调（调色板质量）	10
全量颜色命名	20
色样界面与用户体验	5
作品预览（调色板实际应用）	15
边界情况处理	10
部署（公开在线链接）	10
README 文档	5
总分	100
